{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "vite",
  "installCommand": "sh -c \"if [ -d 'frontend/react-project' ]; then cd frontend/react-project && npm install; else npm install; fi\"",
  "buildCommand": "sh -c \"if [ -d 'frontend/react-project' ]; then cd frontend/react-project && npm run build && mkdir -p ../../dist && cp -r dist/* ../../dist/; else npm run build; fi\"",
  "outputDirectory": "dist"
}
